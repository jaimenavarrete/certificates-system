@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Autenticación en dos pasos";
}

<h2 class="card-title h4 text-primary fw-bold border-bottom border-2 border-primary mb-2">@ViewData["Title"]</h2>

<div class="text-center py-4">
    <p>Su inicio de sesión está protegido con una app de autenticación. Ingrese el código de autenticación en el cuadro de texto de abajo.</p>
    <div class="row d-flex justify-content-center mb-3">
        <div class="col-md-4">
            <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
                <input asp-for="RememberMe" type="hidden" />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-floating mb-3">
                    <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                    <label asp-for="Input.TwoFactorCode" class="form-label"></label>
                    <span asp-validation-for="Input.TwoFactorCode" class="text-danger"></span>
                </div>
                <div>
                    <div class="form-check mb-3">
                        <label class="form-check-label" asp-for="Input.RememberMachine" class="form-label">
                            <input class="form-check-input" asp-for="Input.RememberMachine"/>
                            @Html.DisplayNameFor(m => m.Input.RememberMachine)
                        </label>
                    </div>
                </div>
                <div>
                    <button type="submit" class="w-100 btn btn-lg btn-primary"><i class="las la-sign-in-alt fs-4"></i> Ingresar</button>
                </div>
            </form>
        </div>
    </div>
    <p>
        ¿No tiene acceso a su dispositivo de autenticación? Puede 
        <a id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">iniciar sesión con un código de recuperación</a>.
    </p>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}