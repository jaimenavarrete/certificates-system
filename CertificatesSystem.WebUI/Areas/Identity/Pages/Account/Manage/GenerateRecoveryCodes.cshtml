@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Generar códigos de recuperación para autenticación en dos pasos (2FA).";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<hr/>

<div class="alert alert-warning mb-4" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Coloque estos códigos en un lugar seguro.</strong>
    </p>
    <p>
        Si usted pierde su dispositivo y no tiene sus códigos de recuperación, perderá el acceso a su cuenta.
    </p>
    <p>
        Generar nuevos códigos de recuperación no cambia las claves usadas en las apps de autenticación. Si desea
        cambiar la clave usada en la app de autenticación, deberá <a asp-page="./ResetAuthenticator">reiniciar sus claves de autenticación.</a>
    </p>
</div>
<div>
    <form class="text-center" method="post">
        <button class="btn btn-danger" type="submit"><i class="las la-cog fs-5"></i> Generar códigos de recuperación</button>
    </form>
</div>
