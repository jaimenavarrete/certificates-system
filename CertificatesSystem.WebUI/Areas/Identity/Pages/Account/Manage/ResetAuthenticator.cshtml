@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Reiniciar clave de autenticación";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<hr/>

<div class="alert alert-warning mb-4" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Si reinicia su clave de autenticación, su app de autenticación no funcionará hasta que lo reconfigure con la nueva clave.</strong>
    </p>
    <p>
        Este proceso deshabilita el 2FA hasta que verifique su app de autenticación. Si no completa la configuración de su app de autenticación, puede perder el acceso a su cuenta.
    </p>
</div>
<div>
    <form class="text-center" id="reset-authenticator-form" method="post">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit"><i class="las la-redo-alt"></i> Reiniciar clave de autenticación</button>
    </form>
</div>
