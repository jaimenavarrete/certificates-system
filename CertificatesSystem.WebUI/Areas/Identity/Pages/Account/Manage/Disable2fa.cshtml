@page
@model Disable2faModel
@{
    ViewData["Title"] = "Deshabilitar autenticación en dos pasos (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<hr/>

<div class="alert alert-warning mb-4" role="alert">
    <p>
        <strong>Esta acción solo deshabilita la autenticación en dos pasos (2FA).</strong>
    </p>
    <p>
        Deshabilitar la autenticación en dos pasos no cambia las claves usadas en las apps de autenticación.
        Si desea cambiar la clave usada en la app de autenticación deberá <a asp-page="./ResetAuthenticator">reiniciar sus claves de autenticación.</a>
    </p>
</div>

<div>
    <form class="text-center" method="post">
        <button class="btn btn-danger" type="submit"><i class="las la-trash fs-4"></i> Deshabilitar 2FA</button>
    </form>
</div>
